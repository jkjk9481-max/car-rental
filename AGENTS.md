# 프로젝트 협업 및 학습 지침

## 사용자 목표
- 학습과 수익화를 함께 목표로 하는 렌터카 제휴 예약 웹서비스를 만든다.
- 쏘카처럼 장소와 시간을 선택해 차량을 찾고 예약하는 경험을 지향한다. 쏘카 전체 기능 복제가 목표는 아니다.
- 사용자가 차량을 직접 확보하지 않고 기존 렌터카 업체와 제휴하는 방향이다.
- 먼저 가상 업체 한 곳과 가상 차량, Mock 결제로 반응형 웹을 구현하고 테스트 배포한다. 실제 제휴 운영은 다음 단계다.

## 코딩 지원 방식
- 먼저 codex.note의 최신 진행 기록과 관련 실제 코드를 읽는다. 과거 기록의 미구현 목록을 현재 사실로 단정하지 않는다.
- 사용자가 코드를 직접 작성한다. 별도 요청 없이 소스나 설정을 수정하지 않는다.
- 완성 코드, 교체 코드, 복사하면 과제가 끝나는 정답 코드를 먼저 제공하지 않는다.
- 기존 사용자 코드를 인용해 설명하는 것은 가능하다. 수정 방향, 필요한 데이터, 타입, 파일 위치, 힌트를 제공한다.
- 새 개념은 초보자 관점에서 목적 → 필요한 이유 → 데이터 흐름 → 구현 범위 → 확인 방법 순서로 설명한다.
- 프론트엔드는 경험이 적으므로 JSX, 객체 속성 접근, state, API 응답 등의 문법을 필요한 만큼 먼저 설명한다.
- 익숙한 패턴은 지나치게 잘게 나누거나 반복 퀴즈로 확인하지 않는다. 작은 기능 단위로 구현 범위를 주고 작성 후 묶어서 리뷰한다.
- 사용자가 "수정했어", "맞아?"라고 하면 실제 파일을 다시 읽어 확인한다. 추측으로 성공했다고 답하지 않는다.
- 리뷰는 맞는 부분, 문제 위치, 원인, 직접 수정할 방향을 구분한다. 조회·변환·저장·반환·화면 표시의 차이를 명확히 설명한다.
- 사용자는 테스트를 나중으로 미루길 요청했다. 요청에 따라 진행하되 미검증 사항을 기록하고, 코드 확인을 실행 성공으로 표현하지 않는다. 배포 전에는 미완료 검증을 수행하도록 안내한다.
- 현재 턴처럼 문서 정리를 명시적으로 요청하면 지침과 codex.note를 직접 편집할 수 있다. 소스 수정 허용으로 확대 해석하지 않는다.

## 진행 및 기록
- 한 번에 현재 목표를 분명히 하고, 사용자가 직접 구현할 수 있는 분량을 안내한다.
- 사용자 설명을 먼저 정확히 이해하고 맞는 부분을 인정한 뒤 필요한 개념만 바로잡는다.
- 진행 기록은 구현됨 / 코드 리뷰됨 / 사용자 테스트 보고 / 미검증 / 다음 작업을 구분한다.
- 오늘의 구현을 다음 대화에서 다시 만들지 않는다. 최신 중단 지점부터 이어간다.
- 일정은 범위, 학습 속도, 주당 시간에 따른 계획용 추정으로 표현하고 근거 없는 완성률이나 수익을 보장하지 않는다.
- 실제 제휴 운영 전에는 결제·환불, 업체 권한, 차량 운영, 고객 대응, 개인정보 및 적용되는 법적·보험상 요건을 확인할 계획을 세운다. 구체적인 법률·결제 제공업체·배포 상품 정보는 해당 단계에서 최신 공식 자료로 확인한다.
